/* eslint-disable strict */
//import api from './api';
import bookmarkList from './bookmark-list.js' 

/*function showHiddenForm(){
  const newBookmark = document.getElementById('bookmark-data');
  if (newBookmark.style.display === 'none'){
    newBookmark.style.display = 'block';
  }else{
    newBookmark.style.display= 'block';
  }
};*/



function main(){
    bookmarkList.bindEventListeners();
}

$(main)

//use store to check items before api intergration 
/*const store = {
    bookmarks: [
      { id: cuid(), title: 'News', url: x, description:y},
      { id: cuid(), title: 'Sports', url: x, description:y},
      { id: cuid(), title: 'Health', url: x, description:y},
      { id: cuid(), title: 'Food', url: x, description:y}
    ],
  };*/

 /* function generateBookmarkElement(FormData){
      let bookmarkTitle = `<span class=`
  }*/
//import api from './api';
//import cuid from 'cuid';
//load previous responses
// function getAnswers(){
  //GET previous responses
//}

//reveal the form because it should load without it showing


//render page with new info
//function renderNew(){
  //render()
//}


//take submissions and make them into an object
/*function onSubmit(bookmarkTitle, url, description, rating){
    const obj ={
    title: bookmark-title,
    url: url,
    text: description,
    rating: rating,
    id: cuid(),
  };
  JSON.stringify(obj);
}*/

//post obj to BASE_URL
//function postObj(){
  // POST obj to BASE_URL
//}



//filter
//delete
//edit description
//render

